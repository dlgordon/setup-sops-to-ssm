{
  "name": "setup-gh",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest --passWithNoTests",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "keywords": [],
  "homepage": "https://github.com/dlgordon/setup-sops-to-ssm",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dlgordon/setup-sops-to-ssm.git"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/tool-cache": "^1.7.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.24.0",
    "eslint": "^7.4.0",
    "jest": "^26.1.0"
  }
}
